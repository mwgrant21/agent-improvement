---
loop: daily-triage
level: 1
paused: false
attempt_cap: 3
budget: soft
last_run: 2026-08-26
runs_since_retro: 9
constrained_scopes: []
---
## State Ownership
<!-- Retrofitted 2026-08-22 adopting the loop-design "state ownership ledger"
     convention (loops/README.md, stolen from lidge-jun/opencodex during
     evaluate-repo). This file was 134 lines at retrofit time; the rows below
     are the honest current growth pattern, not aspirational caps. -->
| Category | Location | Cap / rotation policy |
|---|---|---|
| runs.jsonl | loops/daily-triage/runs.jsonl | Unbounded, append-only by design - source of truth for retrospectives. No cap; rotate via log-archivist if reads become slow. |
| Adjustment ledger | STATE.md body | Unbounded rows, one per proposed refinement; landed rows never deleted (re-checked each retrospective per R1). Rotate older LANDED rows to a dated archive via log-archivist if STATE.md exceeds ~50KB. |
| Human Decisions | STATE.md body | Unbounded append-only; retired decisions kept (not deleted) so a future run doesn't misread absence as loss. Same log-archivist rotation trigger as the Adjustment ledger. |
| Watch List | STATE.md body | Bounded by active repo/branch count (~20 currently); an item is pruned to "Resolved since last run" once it closes, so this section self-bounds in practice. |
| High Priority | STATE.md body | Small by construction - an item here is actively awaiting a human decision; moves to Human Decisions or Resolved once decided. |
| Recent Noise / Untriaged noise | STATE.md body | Small, cleared as items get `[FP]`-marked or decided; self-bounding. |
| Resolved since last run | STATE.md body | Pruned every run per existing step-2 convention; prior entries live in git history, not here. |
| constrained_scopes | STATE.md frontmatter | Empty by default; human-added/removed only (Intervention ladder step 2) - naturally small. |

## Constrained Scopes
<!-- Human-added/removed only (loops/README.md, Intervention ladder step 2).
     Each entry: {scope, reason, since, reconsider}. Empty currently -->
(none currently)

**Retrospective completed 2026-08-17** (runs 11-20). `runs_since_retro` now 9 (runs 21-29). Run 24 was a deliberate, human-requested SECOND run on 2026-08-21, taken specifically to exercise the `record-behind-by-alongside-ahead-by` change applied earlier the same day. **Run 25 (2026-08-22) is the first run in this window FROM home-matt** (hostname TITAN) rather than work-it - the machine-tag freeze/verify directionality flipped correctly with no LOOP.md change needed. **Run 26 (2026-08-22, 22:38 MDT) is a deliberate human-requested SECOND same-day run, also from home-matt/TITAN** - the spend threshold fired for the first time in this window. **Run 27 (2026-08-24, home-matt/TITAN)** - cache flagged for the first time this window, and a new repo (`pocket-rogues-grinder`) appeared. **Run 28 (2026-08-25, home-matt/TITAN)** - a fleet-wide read-only `git fetch --prune` (run 27 fetched only one repo) refreshed stale remote-tracking refs and surfaced a large previously-invisible exposure in `tarot`; four Watch List items closed the same run. **Run 29 (2026-08-26, home-matt/TITAN)** - the quietest run of the window and the first ever with an EMPTY High Priority section AND an empty Untriaged-noise section; every run-28 finding the human authorised was verified closed live. Its one new finding is a probe gap: local branches with NO upstream at all have been invisible to the `[gone]` dead-weight check for all 28 prior runs.

## High Priority (waiting on human)
**EMPTY this run - a first for this loop.** Run 28's sole High Priority item
(`tarot`'s 5 orphaned pre-rewrite branches, 1,771 commits on no remote) was
authorised by the human and executed out-of-band on 2026-08-25: bundled to
`C:\Users\Matt\backups\tarot-prerewrite-2026-08-25.bundle` (1.9 GB, `git bundle
verify` clean) and only then deleted. **Verified live this run: `tarot` now has
`master` only, 0 unpushed, and a clean tree apart from one untracked file.** No
new High Priority finding this run - no repo is >20 commits ahead of any remote,
and no escalation is open. See Human Decisions for the full closure record.

## Retrospective outcome (2026-08-06, runs 1-10)
- Refinements 1-6 APPROVED and applied to LOOP.md via the loop-design skill: loop-derived noise counting (1), revised spend/cache thresholds (2), discovered scan roots + source-unavailable reporting (3), machine-tagged hygiene items (4), stale-only uncommitted-changes reporting (5), and a verified commit/push of the loop's own state as step 5 (6).
- Refinements 7 (branch staleness by commits-ahead rather than tip date) and 8 (cache PR/issue results for quiet repos) were HELD, then approved and applied the same day - see the ledger.
- L2 promotion HELD at the user's decision despite the gate being literally met. Re-evaluated by the 2026-08-17 retrospective: NOT MET (see below).
- Refinement 9 APPROVED and applied the same day: the retrospective now reconciles the Adjustment ledger (below) as its FIRST step, per-run critiques record a structured `notes.adjustment` entry, and an adjustment proposed `attempt_cap` (3) times without landing escalates instead of being re-proposed.

## Retrospective outcome (2026-08-17, runs 11-20)
- **Step R1 (ledger reconciliation)**: all 12 rows marked LANDED were re-verified by grepping the CURRENT `LOOP.md` text (not by trusting the ledger table) - all 12 confirmed present, none regressed. Ledger fully cleared the same day across 6 human decisions.
- **Step R2/R3**: all outstanding items decided - 4 applied, 1 held, 1 declined. See Adjustment ledger and Human Decisions.
- **L2 promotion gate, explicitly re-evaluated**: NOT MET, and for the first time a real (not silent-zero) measurement. `false_positives` non-zero on 3 of runs 11-20 (=3, above the <=2 gate) and 2 unresolved escalations open at the time against a gate requirement of 0. Recommendation: do not propose promotion.

## Adjustment ledger
<!-- Seeded 2026-08-06 from the prose critiques of runs 1-10, which predate the structured notes.adjustment field. Retrospective step R1 reconciles this every 10th run by grepping LOOP.md and scripts/ - never by trusting this table's own text. Landed rows STAY here and get re-checked; a landed row that goes missing is REGRESSED and escalates. -->
| id | first_proposed | times | status |
|---|---|---|---|
| batch-branch-commit-date-lookups | 2026-07-14 | 5 | LANDED 2026-08-03 (branch_tips cache, step 1) |
| fix-spend-summary-date-window | 2026-07-17 | 1 | LANDED 2026-07-22 (scripts/spend-summary.mjs local-day bucketing) |
| record-output-token-baseline | 2026-07-17 | 1 | LANDED 2026-07-18 (step 3 notes) |
| flag-branches-20-commits-ahead | 2026-07-18 | 1 | LANDED 2026-08-06 (step 1, local hygiene - promotes a >20-ahead branch to High Priority). **First fired on a genuinely new finding by run 28 (`tarot`, 5 branches x 333-365 commits).** |
| branch-staleness-by-commits-ahead | 2026-07-21 | 1 | LANDED 2026-08-06 (step 1, GitHub - implemented as author-date staleness + `ahead_by` as a separate signal, NOT as the literal "replace date with commits-ahead") |
| verify-loop-own-commit-completed | 2026-07-24 | 1 | LANDED 2026-08-06 (retro refinement 6, step 5) |
| self-confirming-noise-without-fp-mark | 2026-07-24 | 1 | LANDED 2026-08-06 (retro refinement 1, step 2) |
| promote-tokenmonitor-pr1-to-human-decisions | 2026-08-03 | 2 | LANDED 2026-08-06 (Human Decisions section) |
| cache-quiet-repo-pr-issue-results | 2026-08-04 | 1 | LANDED 2026-08-06 (step 1, GitHub - implemented by ELIMINATING the per-repo sweep via one fleet-wide `gh search prs`/`gh search issues` call, NOT by caching a quiet-repo negative) |
| drop-bare-uncommitted-changes-signal | 2026-08-04 | 1 | LANDED 2026-08-06 (retro refinement 5, step 1) |
| machine-tag-watchlist-items | 2026-08-06 | 1 | LANDED 2026-08-06 (retro refinement 4, step 2) |
| exclude-default-branches-from-staleness | 2026-08-06 | 1 | LANDED 2026-08-06 (step 1, GitHub) |
| gate-cache-flag-on-min-volume | 2026-08-07 | 3 | **DECLINED 2026-08-17 (human decision).** Escalated at attempt_cap; 3-of-5 low-volume data points against the hypothesis. Not re-propose-eligible absent new evidence. |
| noise-match-on-finding-identity-not-text | 2026-08-10 | 1 | **LANDED 2026-08-17.** Step 2 now matches on finding identity (repo + branch/path + recommendation, volatile fields like age stripped) instead of literal rendered text. |
| distinguish-broken-probe-from-dead-source | 2026-08-11 | 3 | **LANDED 2026-08-17.** Step 1 distinguishes a dead/absent source from a multi-target source where 100% of targets fail the same way. |
| specify-branch-tips-cache-key-format | 2026-08-11 | 2 | **LANDED 2026-08-17.** Canonicalized on the FULL 40-char SHA; a shorter cached `sha` is a miss. |
| record-behind-by-alongside-ahead-by | 2026-08-21 | 1 | **LANDED 2026-08-21 (human decision, applied via loop-design skill).** Step 1 records `behind_by` from the SAME compare call that already returns `ahead_by` (zero extra API calls), reports the pair as `Na/Mb`, and requires a divergent branch (ahead>0 AND behind>0) to be labelled `divergent, verify against the default branch before proposing a merge`. |
| clarify-repo-discovery-depth-definition | 2026-08-18 | 3 | **LANDED 2026-08-21 (human decision, applied via loop-design skill).** LOOP.md step 1 pins depth to the REPO DIRECTORY (`find <root> -maxdepth 3 -name .git`), names `~/Downloads` as a third scan root, and classifies a `.git` FILE as a linked worktree attributed to its parent. |
| freeze-unchanged-runs-when-not-verified | 2026-08-21 | 1 | **LANDED 2026-08-21 (human decision, applied via loop-design skill).** A run that does not OBSERVE a repo carries `unchanged_runs` forward frozen with `frozen_not_verified: true`. |
| dedupe-same-day-spend-baseline | 2026-08-21 | 1 | **LANDED 2026-08-21 (human decision, applied via loop-design skill).** Spend/cache figures identical to the previous run-log line are a RE-READ: tagged `spend_duplicate_of`, excluded from the trailing-5 median. |
| dirty-count-blind-to-content-churn | 2026-08-13 | 1 | **LANDED 2026-08-17.** Staleness check compares the dirty file PATH SET, not the line count; `dirty_lines` -> `dirty_paths`. |
| expand-home-matt-discovery-root | 2026-08-15 | 1 | **HELD 2026-08-17 (human decision).** Rests on a single home-matt-only observation unverifiable from work-it. Re-propose with home-matt confirmation. |
| validate-jsonl-line-before-append | 2026-08-17 | 1 | **LANDED 2026-08-17.** Round-trip `JSON.parse(JSON.stringify(...))` before appending a run's own line. |
| worktree-hygiene-report | 2026-08-21 | 1 | **LANDED 2026-08-21 (human decision, applied via loop-design skill, same day as proposed).** Report-only worktree staleness check. |
| gitignore-and-auth-drift-check | 2026-08-21 | 1 | **LANDED 2026-08-21 (human decision, applied via loop-design skill, same day as proposed).** Per-repo `.claude/worktrees/` gitignore drift check plus a once-per-run fleet-wide `gh auth status`. **Both gaps it opened (aether-os, TokenMonitorV2) are CLOSED as of run 28.** |
| close-expand-home-matt-discovery-root | 2026-08-22 | 1 | **PROPOSED by run 25 - not yet a human decision.** `~/projects` is ALREADY reached by the existing `~` scan root at the repo-dir depth-3 rule, so the HELD `expand-home-matt-discovery-root` adjustment is moot and needs no LOOP.md change - proposing to CLOSE it. Re-confirmed by runs 26, 27, and 28 (28 found 21 repos on home-matt, all under already-reached roots; nothing outside them). |
| clarify-unchanged-runs-flag-threshold | 2026-08-24 | 1 | **PROPOSED by run 27 - not yet a human decision.** Run 26 flagged `TarotApp` as loop-derived noise at `unchanged_runs==3` but left `tarot`, `Miriels-publish`, and `About-me` unflagged at the identical value. Propose pinning one reading in LOOP.md so the noise rule fires identically for every item at the same counter value. |
| fetch-prune-before-unpushed-check | 2026-08-25 | 1 | **PROPOSED by run 28 - not yet a human decision.** The local-hygiene unpushed check reads `git log --branches --not --remotes`, whose answer is only as good as the clone's remote-tracking refs. LOOP.md permits a read-only `git fetch` as the Resync tier (loops/README.md, Operational failure ladder step 3) but never says WHEN to run it, so runs 24-27 ran it ad hoc on a single repo or not at all - and stale refs made `tarot`'s 5 orphaned branches (1,771 commits total, on no remote) read as fully pushed. Propose: run `git fetch --prune` on every discovered repo with a remote BEFORE the unpushed/`[gone]` check, and record `refs_refreshed: true` in `notes` so a future reader can tell a fresh answer from a stale one. Stays read-only (fetch, never pull/merge), so the L1 boundary is unchanged. |
| detect-no-upstream-local-branches | 2026-08-26 | 1 | **PROPOSED by run 29 - not yet a human decision.** The local-hygiene dead-weight check only sees a branch whose upstream was configured and then DELETED (`[gone]`). A local branch with NO upstream at all is invisible to it, and has been for all 28 prior runs. Run 29 found 5 such branches - `TokenMonitor` 4 (`design-v2-phase2`, `design-v2-phase4`, `plan-aware-usage`, `req-1-risk-axis-gate-check-rule-and-companion-optimize-ui-polish`) and `code-graph-mcp` 1 (`worktree-code-graph-mcp-impl`) - each measured at 0 commits not on a remote, i.e. fully absorbed dead weight. The 2026-08-25 fleet-wide cleanup could not have caught them because nothing ever reported them. Propose: enumerate every local branch, classify as tracked-and-live / tracked-but-gone / untracked, and report the last two together as dead weight when `git log <branch> --not --remotes` returns 0. |

Ledger standing (last full reconciliation 2026-08-17, plus same-day-as-proposed landings since): **23 of 28 landed**, 1 declined (`gate-cache-flag-on-min-volume`), 1 held (`expand-home-matt-discovery-root`, run-25/26/27/28/29 evidence suggests closing it), 4 new proposals awaiting decision (`close-expand-home-matt-discovery-root`, `clarify-unchanged-runs-flag-threshold`, `fetch-prune-before-unpushed-check`, `detect-no-upstream-local-branches`), 0 escalated at attempt-cap.

Still genuinely pending a human decision: `close-expand-home-matt-discovery-root` (first_proposed 2026-08-22, 1x, from run 25), `clarify-unchanged-runs-flag-threshold` (first_proposed 2026-08-24, 1x, from run 27), `fetch-prune-before-unpushed-check` (first_proposed 2026-08-25, 1x, from run 28 - run 29 again ran the fetch ad hoc, which is exactly the inconsistency it exists to remove), and `detect-no-upstream-local-branches` (first_proposed 2026-08-26, 1x, NEW from run 29). None re-proposed this run, so none has advanced toward the attempt cap. A fifth candidate is implied by the 2026-08-25 stale-`index.lock` finding but is deliberately NOT proposed yet - run 29 swept the fleet for `.git/*.lock` ad hoc and found none, so there is one clean observation behind it and no second data point.

Note for the next retrospective's step R1: two rows deviate from their original proposal text ON PURPOSE, and each row says how - `branch-staleness-by-commits-ahead` became author-date staleness plus a separate `ahead_by` signal, and `cache-quiet-repo-pr-issue-results` became a fleet-wide search that removes the calls rather than a cache of their answers. Both would read as never-landed under a naive text match. Also: when reading `runs.jsonl` in full for R1, line 27 is invalid JSON - use a tolerant per-line parse that logs-and-skips rather than aborting.

## Watch List
- **NEW - 5 local-only branches with NO upstream, invisible to every prior run.** The dead-weight check keys on `[gone]` (an upstream that was configured then deleted); a branch that never had an upstream is not `[gone]` and was never reported. All 5 measured live this run at **0 commits not on any remote** - fully absorbed. `TokenMonitor`: `design-v2-phase2` (3208d9a), `design-v2-phase4` (fbe19cb), `plan-aware-usage` (fae5d8b), `req-1-risk-axis-gate-check-rule-and-companion-optimize-ui-polish` (c15e058). `code-graph-mcp`: `worktree-code-graph-mcp-impl` (36bb4aa). TokenMonitor v1 is FINAL per the 2026-08-25 decision, so its 4 are end-of-life state rather than work needing attention - but they are still safe-to-delete clutter the 2026-08-25 sweep would have taken had anything reported them. [action: safe to `git branch -d` all 5; the loop never deletes them itself] [machine: home-matt]
- **Cache-rate FLAG: today's hit rate is 86.3%**, below BOTH floors (0.90 absolute, and 0.886 = trailing-5 median 0.936 minus 5pp). Second fire in three runs (run 27 at 87.2%, run 28 clean at 93.6%). Today's total output is only **11,371 tokens**, most of it this triage itself - a near-empty day where a cold context load dominates the ratio. `gate-cache-flag-on-min-volume` was DECLINED 2026-08-17 and is deliberately NOT re-proposed; the flag is reported exactly as the threshold defines it. [action: none unless it fires again on a HIGH-volume day, which would mean something real] [machine: any]
- **`TarotApp` (home-matt) still holds the same 3 dirty paths - but this is the FIRST observation of them since the blocking `.git/index.lock` was removed.** `app/build.gradle`, `MainActivity.kt`, `app/src/main/res/raw/images_oracle.zip`, now 6 consecutive identical observations. The prior 5 happened while a stale 2026-08-12 lock made committing impossible, so they were not evidence of a decision. This one is. NOT noise, per the standing 2026-08-25 decision that TarotApp must stay current. **Its divergence is RESOLVED**: `master` is level with `origin/master` at `941b7de`, 0 unpushed. [action: commit or discard these 3 now that the repo can actually commit] [machine: home-matt]
- **`~/.claude` (dotclaude, home-matt) holds the identical 61-path dirty set as run 28** - the 2026-08-25 agent-fleet overhaul is still entirely uncommitted. `unchanged_runs` 0->1. 0 unpushed commits, tree level with `origin/master`. Two more identical observations would graduate it to noise, which for a 61-path surface would be the wrong outcome. [action: commit the overhaul, or split it - it is a large uncommitted surface whose only copy is this disk] [machine: home-matt]
- **`aether-os` (home-matt) dirty set identical for a 4th run** (same 10 paths across runs 26-29), `unchanged_runs` 2->3 - **crosses the loop-derived noise line this run** and is counted as such below. [action: a one-line decision retires it the way tarot/About-me/TokenMonitor were retired on 2026-08-25] [machine: home-matt]
- **Fleet PR/issue counts: 0 open PRs, 1 open issue.** Both live-verified (`gh search`, `--limit 100`, neither truncated; 24 repos enumerated). [machine: any]
  - `Aether-OS#22` white screen after desktop lock. `updatedAt` still 2026-08-12 - now **14 days idle**. Deliberately left open and still instrumented. [action: none until it recurs - then grab the dev output and grep `[diag]` BEFORE closing the window] [machine: any]
- **Local clones lagging their remotes (read-only observation, no risk):** `Claude-Files` behind 17, `nmmtools` behind 8 (both unchanged), and **newly `.agency-agents` behind 14** - a non-`mwgrant21` remote (`msitarzewski/agency-agents`), outside fleet PR/issue scope. All three clean trees, 0 unpushed. [action: `git pull` when next working in them] [machine: home-matt]
- **`agent-improvement` candidate buffer (home-matt) grew from 56 to 59 pending lines** since the 2026-08-22 promote pass - normal accumulation, not stale. [action: none] [machine: home-matt]
- **`tarot` retains one untracked file, `GPU-CRASH-NOTES.md`** - deliberately kept per the 2026-08-25 decision, and still the only copy of that investigation anywhere. The dirty-path set went 4 -> 1, so this is a CHANGED set and its `unchanged_runs` is re-baselined to 0, not carried forward. [action for a human: commit it so the record stops being one `rm` from gone] [machine: home-matt]
- Literal tilde-named directory at `C:\Users\Matt\~\vexjoy-agent` - discovery still finds a `.git` there, but `git -C` cannot open it (the literal `~` path segment does not resolve), so its state is UNVERIFIABLE by this loop's own probe rather than clean. Unchanged from run 28. [action: none required, worth a rename if unintentional - a rename would also make it probe-able] [machine: home-matt]
- `.agency-agents` and `claude-power-automate` (home-matt) - clean trees, non-`mwgrant21` remotes, outside fleet PR/issue scope, re-verified this run. [action: none] [machine: home-matt]
- `agent-improvement` store (home-matt) - clean and level with `origin/master` at the start of this run. [action: none] [machine: home-matt]
- **NOT VERIFIABLE THIS RUN (machine flip - these are work-it-only paths, frozen not incremented):** `Aether-OS-livetest` (5 dirty paths, `unchanged_runs` frozen at 1), `NMMToolkit` (29 dirty paths, frozen at 1), `uw-mail-router` Downloads clone (1 dirty, frozen at 1), the `Desktop\cli-shared-memory-agents` worktrees, `uw-router-teams-tab`, `EFIPartitionRemediation`/`RoundRobin`/`it-claude-marketplace`/`claude-config` local state, and the Desktop stray `.git`. GitHub-side branch/PR/issue data for all of these is fresh (fleet-wide calls are machine-agnostic); only their LOCAL dirty/unpushed state is unverifiable from home-matt. [action: none, resume verification next work-it run] [machine: work-it]

## Recent Noise (ignored this run)
<!-- Mark an item [FP] if it was a false positive. Refinement 1: the loop ALSO counts an item whose FINDING IDENTITY (volatile fields like age stripped) is unchanged across 3 consecutive runs with no human action as noise on its own evidence, without waiting for a mark. -->
- **loop-derived noise (NEW this run): `aether-os` (home-matt) - same 10 dirty paths, 4th consecutive identical observation** (runs 26, 27, 28, 29), no human action. Crosses the 3-run line. [fp_source: loop_derived] [machine: home-matt]
- **human-marked (2026-08-25): `tarot` dirty paths - ACCEPTED as deliberate.** Moot as of this run anyway: the GPU-debug leftovers were reverted and only the untracked `GPU-CRASH-NOTES.md` remains. [fp_source: human_marked] [machine: home-matt]
- **human-marked (2026-08-25): `About-me` `README.md` - FROZEN by explicit request.** 6th consecutive identical observation, suppressed by decision, not re-raised. [fp_source: human_marked] [machine: home-matt]
- **human-marked (2026-08-25): `TokenMonitor` (v1) `.archex/` - v1 declared FINAL.** 5th consecutive identical observation, suppressed by decision. [fp_source: human_marked] [machine: home-matt]
- **`TarotApp` is NOT counted as noise** - rejected as such by the 2026-08-25 decision and reinstated as a real Watch List finding above, now resting on materially different evidence (the `index.lock` is gone).
- Spend threshold: today's **11,371** output tokens - nowhere near the 750,000 absolute floor. No flag.
- Cache threshold: **FLAGGED** at 86.3% - see Watch List.
- `gate-cache-flag-on-min-volume` was DECLINED 2026-08-17 and is deliberately NOT re-proposed, despite this run being exactly the low-volume shape it described.

## Untriaged noise
- **NONE. Empty for the first time in this loop's history.** The one item still outstanding after run 28 - `Miriels-publish`'s 3 dirty paths, unmarked for 3 runs - **self-resolved rather than needing a decision**: the GPU-debug leftovers were reverted on 2026-08-25 and the tree is now fully clean, 0 unpushed. Every other previously-untriaged item was explicitly decided in the 2026-08-25 pass.
- Worth carrying to the next retrospective: of the 5 items that reached "untriaged after 2+ runs" in this window, 3 were accepted, 1 was rejected as a genuine noise-rule miss (`TarotApp`), and 1 resolved on its own. The rule surfaced them correctly; what it could not do was tell "nothing moved because it is settled" apart from "nothing moved because the repo was broken" - which is what `TarotApp`'s `index.lock` turned out to be.

## Human Decisions (overrides the loop must respect)
- **2026-08-25 (post-run-28): run 28's High Priority and dead-weight findings AUTHORIZED and EXECUTED out-of-band.** L1 boundary intact - the loop reported, the human authorized each item explicitly, a separate session acted. Do not re-raise any of these.
  - **`tarot` orphaned pre-rewrite history - BUNDLED, then DELETED.** All 5 branches verified first: `git merge-base` empty against `origin/master` for every one. Bundled to `C:\Users\Matt\backups\tarot-prerewrite-2026-08-25.bundle` (**1.9 GB** - the deck art is in that history), `git bundle verify` reporting "okay / complete history" with all 5 tips matching (`b4b9fc0`, `836d594`, `b0bbc3c`, `4b824c0`, `ef7924c`). Only then deleted with `-D`. **`tarot` is now `master` only, 0 unpushed.** The bundle is the sole surviving copy - if it is ever moved or deleted, the history is gone.
  - **11 `[gone]` dead-weight branches DELETED** across `TarotApp` (5), `TokenMonitor` (4), `aether-os` (1, closing the 2026-08-21 cleanup residue), `nmmtools` (1). Each was re-verified at 0 commits not on a remote immediately before deletion, not trusted from the run-28 digest.
  - **`TarotApp` divergence RESOLVED - merged and pushed (`37a480a..941b7de`).** The investigation found the `behind 1` was a phantom: remote `37a480a` and local `f84f60a` are DUPLICATE merges of the same two parents (`2b9dac0` + `8bcbb30`) with **byte-identical trees** (`c796c496...`). The remote commit contributed zero content; local was strictly ahead. The merge left the tree unchanged (`5a116bce...` before and after) and the push was then a clean fast-forward. **Note for future runs: `ahead/behind` correctly flagged this as not-fast-forwardable, but "behind" did NOT mean "the remote has work you lack" - it meant the graphs had diverged around identical content. The pair is the right signal; it just does not distinguish graph divergence from content divergence, and only a tree comparison settles that.**
  - **ROOT CAUSE FOUND for `TarotApp`'s 5-run-identical dirty paths: a stale, empty `.git/index.lock` dated 2026-08-12**, with no git process running - 13 days old. It had been blocking every index-modifying operation in that repo the entire time. Removed. **This reframes the whole finding: the loop read "3 dirty paths, unchanged for 5 runs" as idle WIP and graduated it to noise, when it was actually a repo that had been unable to commit since Aug 12.** A fleet-wide sweep for `index.lock`/`HEAD.lock`/`config.lock` found no others. Strong candidate for a new step-1 check - see the adjustment note below.
  - **`Miriels-publish` and `tarot` GPU-debug leftovers DISCARDED 2026-08-25 (user-authorized).** Electron 43.x was confirmed a genuinely bugged release and the work has moved on, so the three probe changes (`fullscreen: true`, `electron . --no-sandbox`, the 10-second `setTimeout` around `checkThreshold()`) were reverted with `git checkout --` in BOTH repos. Diffs preserved first at `C:\Users\Matt\backups\{tarot,Miriels-publish}-gpu-debug-leftovers-2026-08-25.patch` (1,400 bytes each). **`Miriels-publish` is now a fully clean tree; `tarot` has only untracked `GPU-CRASH-NOTES.md` left.** Both 5-run noise findings are genuinely CLOSED, not suppressed.
  - **`tarot/GPU-CRASH-NOTES.md` DELIBERATELY KEPT.** It is UNTRACKED, not modified - a 78-line investigation record (driver build mismatch, `STATUS_BREAKPOINT` exit codes, everything ruled out) that exists in no commit anywhere, so it is its own only copy. Left in place. **It also does not match the summary elsewhere that "43.x is broken, 35.x works": the notes record the crash occurring on Electron 35.7.5, on the same machine where aether-os's identical 35.x renders fine.** [action for a human: commit this file so the investigation record stops being one `rm` from gone] [machine: home-matt]
  - **FACT CORRECTION for future runs: neither `tarot` nor `Miriels-publish` has moved off Electron 35.** Both still declare `"electron": "^35.0.0"` in `package.json` at HEAD, and neither has `node_modules` installed. Whatever move-to-latest happened, it did not land in these two repos. Do not assume the fleet is uniformly on a current Electron.
  - **`Miriels-publish` diff INSPECTED before the discard above.** Not boilerplate: the 3 paths are unfinished GPU-crash-investigation leftovers - `electron/main.js` adds `fullscreen: true`, `package.json` changes the electron script to `electron . --no-sandbox`, and `public/js/bootstrap.js` wraps `checkThreshold()` in a 10-second `setTimeout`. `tarot` carries the same three plus `GPU-CRASH-NOTES.md`. Run 27's guess that this was "one fix that should land in both repos" was half right - it is one CHANGE SET in both, but it is a debug experiment, not a fix, and `--no-sandbox` is a security-relevant thing to leave uncommitted and un-decided.
- **2026-08-25 (post-run-28): project-status decisions given directly by the user, covering 4 of the 5 untriaged noise items.**
  - **`tarot` - project is COMPLETE** apart from small code tweaks; every deck and image is in place. The 4 dirty paths (`electron/main.js`, `GPU-CRASH-NOTES.md`, `package.json`, `public/js/bootstrap.js`) ARE those small tweaks - deliberate, accepted. **Do not re-raise the dirty-path finding.** This decision covers the WORKING TREE only; the orphaned pre-rewrite branches in High Priority are a separate, still-undecided question and are NOT closed by it.
  - **`About-me` - FROZEN by explicit request.** The 1 dirty path (`README.md`) is deliberate and must be left exactly as it is. The user still intends video demos and a refinement pass on it, and has said not to alter it in the meantime. **Do not re-raise, and never propose committing or discarding it.**
  - **`TokenMonitor` (v1) - FINAL.** v2 (`TokenMonitorV2`) is the current line of work. v1's `.archex/` dirty path, its 4 `[gone]` branches, and `req-12-stryker-ci-gate` (`1a/0b`) are all end-of-life state on a frozen project. **Do not raise v1 findings as things needing attention**; report v1 only if something genuinely CHANGES there, which on a final project would itself be the news.
  - **`TarotApp` (Android) - NOT NOISE, RECLASSIFIED.** The user requires it to stay current and states the last version they have is the correct one. That makes the 3 local-only commits on `master` (`ded153e` Carried Set rename + 1.14 bump, `7dcf7d2` Given Ground + Lenormand parity, `f84f60a` merge of `origin/android-deck-parity`) the authoritative version, existing on no remote - real exposure, not idle WIP. **Remove `TarotApp` from loop-derived noise and keep its divergence on the Watch List until the clone is reconciled with `origin/master`.** Note the divergence is `3 ahead / 1 behind`: the remote holds one commit the local clone lacks, so "the last version we have" is not the whole history and a plain push will not work. **DIVERGENCE CLAUSE SATISFIED AND RETIRED 2026-08-26 (run 29)**: `master` is level with `origin/master` at `941b7de`, 0 unpushed, live-verified after a `fetch --prune`. The not-noise clause STANDS - TarotApp's 3 dirty paths remain a real Watch List finding, not noise.
  - **`Miriels-publish` - was STILL UNDECIDED at the time of this pass; CLOSED 2026-08-26 (run 29) WITHOUT needing a decision.** Its 3 dirty paths were the GPU-debug leftovers, reverted the same day under the authorisation above. The tree is now fully clean, 0 unpushed - live-verified. No human call is owed.
- **2026-08-22: `TokenMonitorV2` branch disappearance - HELD, pending Monday 2026-08-24.** Superseded by the follow-up below.
- **2026-08-24 FOLLOW-UP, RESOLVES THE ABOVE: `TokenMonitorV2` confirmed ACTIVE on work-it, not silently trimmed.** Merged cleanly and pushed (`5a76f39`). This is real, ongoing work-it activity, not evidence of unauthorized branch loss - the routine-cleanup explanation stands confirmed. RESOLVED, do not re-raise. **Run 28 confirms `TokenMonitorV2` `main` on GitHub is now exactly `5a76f39`, matching that push, and the home-matt clone is clean and level.**
- TokenMonitor PR #1 ("Terminal project folder + repo CLAUDE.md") was the user's own active PR. **MERGED**; override satisfied and retired. Recorded rather than deleted so a future run does not read its absence as the decision having been lost.
- **2026-08-17: adjustment `gate-cache-flag-on-min-volume` DECLINED.** Treat as closed; do not re-propose absent materially new evidence.
- **2026-08-17: adjustment `distinguish-broken-probe-from-dead-source` APPLIED.** User delegated the accept/decline call to the agent.
- **2026-08-17: remaining 5 OUTSTANDING adjustments decided in one pass.** 4 APPLIED, 1 HELD (`expand-home-matt-discovery-root`).
- **2026-08-21: `Aether-OS` branch cleanup APPROVED and EXECUTED out-of-band** (L1 boundary intact). Went from 17 local + 4 remote branches to 1 + 1 (`master` only, both sides). A future run must treat ANY new branch there as new work; the baseline is zero. **Run 28: GitHub still shows `master` only. The one local `[gone]` branch (`real-projects-view-stage16`, 0 unpushed) is residue of this cleanup, not new work.**
- **2026-08-21: fleet-wide branch cleanup APPROVED and EXECUTED out-of-band.** 8 local branches deleted across `TokenMonitorV2` and `claude-token-tracker`; `TokenMonitor/chore/electron-43.4.1` deleted from the remote by the human directly. Do not re-raise.
- **2026-08-21: `TokenMonitor` `worktree-packages-core-wiring` ABANDONED.** Merge attempted, investigated, REFUSED as unsafe. Local branch `-D`'d by design, remote branch deleted, worktree removed. **Commit preserved for recovery: `43fde10ab80909e50cf0d6c2768a96d29108ac28`.** Do not re-raise.
- **2026-08-21: `EFIPartitionRemediation` `feature/fleet-migration-runbook` MERGED** (fast-forward `e5f1ca7..c77a650`, pushed). **The branch was deliberately NOT deleted** - it is checked out in the live worktree `~/Desktop/EFI-wt-migration`. Run 28 measures it unchanged at `0a/0b`, author-date 2026-08-06 (19 days - it WOULD cross the staleness line). **It is suppressed by this decision and must not be re-raised as a finding.** Its continued existence and checkout are deliberate.
- **2026-08-21: `cli-shared-memory` (home-matt) untracked `.claude/` - DECISION IS `commit it`.** **SATISFIED AND RETIRED 2026-08-25 (run 28)**: a run FROM home-matt observed the path set gone - `cli-shared-memory` is now a fully clean tree, 0 unpushed, and its `.claude/worktrees/git-arbiter-plan` worktree is also gone. This is exactly the closure condition the decision itself specified. Recorded rather than deleted.
- **2026-08-21: adjustments `worktree-hygiene-report` and `gitignore-and-auth-drift-check` APPLIED**, outside the normal retrospective cycle, by direct human request. All three new findings stay within the L1 report-only boundary. **Run 28: both gitignore gaps these opened are now CLOSED, and the one stale worktree the worktree check ever found is gone - the checks fired, the human acted, the findings retired. Keep the checks; they are cheap and now proven end-to-end.**

## Resolved since last run
<!-- Pruned each run per step 2. Prior entries remain in git history. -->
- **`tarot` orphaned pre-rewrite history (run 28 High Priority) - RESOLVED.** Bundled to `C:\Users\Matt\backups\tarot-prerewrite-2026-08-25.bundle` (1.9 GB, `git bundle verify` clean, all 5 tips matched), then deleted with `-D`. Live-verified this run: `tarot` has `master` only, 0 unpushed, level with `origin/master`. The bundle is the sole surviving copy of that history.
- **`TarotApp` `master` divergence `3a/1b` - RESOLVED.** Merged and pushed (`37a480a..941b7de`); the `behind 1` was a duplicate merge with a byte-identical tree, so the merge changed nothing and the push fast-forwarded. Local now level with origin, 0 unpushed.
- **All 11 `[gone]`-tracking branches - RESOLVED. 0 remain fleet-wide** across all 20 probe-able repos (`TarotApp` 5, `TokenMonitor` 4, `aether-os` 1, `nmmtools` 1, all deleted 2026-08-25 after re-verification at 0 unpushed).
- **`Miriels-publish` 3 dirty paths - RESOLVED.** Tree fully clean, 0 unpushed. This was the last item sitting in Untriaged noise, and it closed without needing a decision.
- **`tarot` dirty path set 4 -> 1 - the noise finding is CLOSED, not suppressed.** Only the deliberately-kept untracked `GPU-CRASH-NOTES.md` remains; `unchanged_runs` re-baselined to 0 because the path SET changed.
- **Stale `.git/index.lock` in `TarotApp` (13 days old, silently blocking every commit since 2026-08-12) - REMOVED, confirmed gone.** A fleet-wide sweep for `.git/*.lock` this run found **0 across all 20 probe-able repos**.
- **Worktree hygiene: 0 stale worktrees fleet-wide** - every probe-able repo reports exactly 1 (main) worktree.
- **Gitignore drift: 0 gaps fleet-wide** - `TokenMonitor`, `TokenMonitorV2`, `aether-os` and `nmmtools` all carry `.claude/worktrees/`; no other repo has a `worktrees/` pattern to drift from.
- `gh auth status`: OK, logged in as `mwgrant21`, active, scopes `gist`/`read:org`/`repo` - no auth-drift finding this run.
- `agent-improvement` domain freshness: newest date across `domains/*.md` is **2026-08-22**, 4 days old - inside the >7-day threshold.
- BRANCH PROBE: **0 failures, 24/24 repo targets returned** (no broken-probe condition). 22 of 24 default-branch tips unchanged since run 28 (cache reused); 2 moved (`TarotApp` `37a480a`->`941b7de`, `agent-improvement` `39fb3e4`->`dda4616`, both author-date 2026-08-26, both excluded from staleness per the default-branch rule). Both non-default branches were unchanged in SHA **and** their default-branch SHA was unchanged, so `ahead_by`/`behind_by` were reused from cache with **zero compare calls**.
- STALENESS: only 2 non-default branches exist fleet-wide. `TokenMonitor/req-12-stryker-ci-gate` is 1 day old, `1a/0b` (clean unmerged work, fast-forward available). `EFIPartitionRemediation/feature/fleet-migration-runbook` is 20 days old at `0a/0b` but SUPPRESSED by the standing 2026-08-21 Human Decision. **Zero reportable staleness findings this run.**
- REPO DISCOVERY: 21 local repos on home-matt, unchanged from run 28. `~`, `~/Desktop` and `~/Downloads` all present; the latter two contain zero git repos (present-but-empty, not absent - no `source unavailable` line is correct). Re-confirms `close-expand-home-matt-discovery-root` for a 5th consecutive run.
- Test-residue defect in `tests/EFIMigrationState.Tests.ps1` (REPORTED, NOT FIXED, carried forward - not independently verifiable from home-matt, no new evidence this run). [action for a human: strip any pre-existing deny ACE in the test's SETUP]
