---
loop: daily-triage
level: 1
paused: false
attempt_cap: 3
budget: soft
last_run: 2026-08-21
runs_since_retro: 3
constrained_scopes: []
---
## Constrained Scopes
<!-- Human-added/removed only (loops/README.md, Intervention ladder step 2).
     Each entry: {scope, reason, since, reconsider}. Empty currently -->
(none currently)

**Retrospective completed 2026-08-17** (runs 11-20 since the 2026-08-06 retrospective). Full results under "Retrospective outcome (2026-08-17, runs 11-20)" below. `runs_since_retro` reset to 0.

## High Priority (waiting on human)
- **`TokenMonitor` / `claude-token-tracker` `worktree-packages-core-wiring` - MERGE ATTEMPTED 2026-08-21 and REFUSED. Do not merge this branch as-is; it regresses master.** Run 23 framed it as "1 ahead, merge or abandon", which understates it: it is 1 ahead and **45 behind**, and that single commit (`43fde10`, 2026-08-04) deletes 788 lines - `src/shared/optimizeRules.js`, `optimizeActions.js`, `optimizeGrade.js`, `modelPricing.js` and their four test files - replacing them with imports from `@tokenmonitor/core`. Two independent blockers, both verified:
  1. **It would delete newer work.** Master has 5 commits to those files since the merge-base (`9dccf73`), including the entire `risky-commands-ran` optimize rule (Risk axis R0-R3, `49c4457`) plus two review-gap fixes on it. `risky-commands-ran` is present in `claude-token-tracker` master (`optimizeRules.js`, `optimizeActions.js`, `renderer/.../optimize.js`) and **absent from every file in `TokenMonitorV2/packages/core`** - so the package it switches to has not absorbed that work. Merging drops the rule and its tests.
  2. **The dependency is machine-local.** The branch adds `"@tokenmonitor/core": "file:C:/Users/IT/Desktop/TokenMonitorV2/packages/core"` - an absolute path into a DIFFERENT repo's working tree on work-it. Landing that on master breaks any clean clone, CI, and home-matt.
  [action for a human: this needs rework, not a merge - either port `risky-commands-ran` into `packages/core` first and re-target the dependency at something resolvable (published package or in-repo workspace), or abandon the branch and redo the extraction from current master. Choosing "abandon" is a legitimate close; leaving it as a standing "merge or abandon" item is not, now that the merge path is known to be unsafe.]
(none currently - both prior items resolved this run, see Resolved below)
- **STILL OPEN: `runs.jsonl` line 27 (run 19, 2026-08-15) is INVALID JSON.** Contains an unescaped backslash in a Windows path inside the critique text (`...no Desktop\Aether-OS clone on home-matt...`) - `\A` is not a legal JSON escape sequence. The 2026-08-17 retrospective recommended leaving it as documented corpus damage rather than hand-editing an append-only historical line (see Adjustment ledger, `validate-jsonl-line-before-append`, which now prevents recurrence going forward). Carried here only as a standing note, not an action item - no further decision needed unless a human wants to hand-fix it. [machine: any]
## Retrospective outcome (2026-08-06, runs 1-10)
- Refinements 1-6 APPROVED and applied to LOOP.md via the loop-design skill: loop-derived noise counting (1), revised spend/cache thresholds (2), discovered scan roots + source-unavailable reporting (3), machine-tagged hygiene items (4), stale-only uncommitted-changes reporting (5), and a verified commit/push of the loop's own state as step 5 (6).
- Refinements 7 (branch staleness by commits-ahead rather than tip date) and 8 (cache PR/issue results for quiet repos) were HELD, then approved and applied the same day - see the ledger.
- L2 promotion HELD at the user's decision despite the gate being literally met. Re-evaluate only after 10 runs in which `false_positives` is actually being fed by refinement 1. **10 of 10 done as of this run** (runs 11-20); run 13 was the first to record a non-zero `false_positives` (1, loop-derived); runs 18 and 19 also recorded 1 (same recurring item - the Aether-OS no-remote branch, now RESOLVED this run, see Resolved below); this run (20) recorded 0. [action: the count the retrospective was waiting on is now complete - the upcoming Retrospective (runs_since_retro=10) should explicitly re-evaluate the L2 promotion gate as part of Step R2]
- Refinement 9 APPROVED and applied the same day: the retrospective now reconciles the Adjustment ledger (below) as its FIRST step, per-run critiques record a structured `notes.adjustment` entry, and an adjustment proposed `attempt_cap` (3) times without landing escalates instead of being re-proposed.

## Retrospective outcome (2026-08-17, runs 11-20)
- **Step R1 (ledger reconciliation)**: all 12 rows marked LANDED were re-verified this run by grepping the CURRENT `LOOP.md` text (not by trusting the ledger table) - all 12 confirmed present, none regressed. Of the 7 remaining rows: `gate-cache-flag-on-min-volume` is reclassified OUTSTANDING -> **ESCALATED** (times_proposed hit `attempt_cap`=3 without anyone noticing the ledger table hadn't been updated to match - see High Priority). `distinguish-broken-probe-from-dead-source` stays ESCALATED (unchanged, still absent from LOOP.md's step 1 source-failure handling). The other 5 (`noise-match-on-finding-identity-not-text`, `specify-branch-tips-cache-key-format`, `dirty-count-blind-to-content-churn`, `expand-home-matt-discovery-root`, `validate-jsonl-line-before-append`) stay OUTSTANDING - confirmed absent from LOOP.md's current text. Ledger now **12 landed / 0 held / 5 outstanding / 2 escalated, 19 total**.
- **Step R2 (proposals)**, numbered for human review - NONE applied this run (no interactive human present in this automated session to approve via the loop-design skill, so R3 made zero edits; all of the below are proposals only):
  1. Confirm or override the ESCALATED `gate-cache-flag-on-min-volume` reclassification above; retrospective recommends DECLINE.
  2. Decide `distinguish-broken-probe-from-dead-source` (apply or HELD-with-reason) - now 4 runs past its own escalation with no decision.
  3. Decide `noise-match-on-finding-identity-not-text` (age 7d, 1x proposed).
  4. Decide `specify-branch-tips-cache-key-format` (age 6d, 2x proposed) - canonicalize on 7-char or 40-char SHA.
  5. Decide `dirty-count-blind-to-content-churn` (age 4d, 1x proposed) - compare the dirty file-path SET, not just the line count.
  6. Decide `expand-home-matt-discovery-root` (age 2d, 1x proposed) - add `~/projects/*` as a machine-conditional discovery root; still unverifiable from work-it (no such root here).
  7. Decide `validate-jsonl-line-before-append` (age 0d, NEW this retrospective, proposed by run 20) - round-trip `JSON.parse(JSON.stringify(...))` before appending a run's own line, abort/report on failure. Directly motivated by `runs.jsonl` line 27.
  8. `runs.jsonl` line 27's invalid JSON: recommend leaving it as documented corpus damage rather than hand-editing an append-only historical line. This retrospective itself is proof the tolerant-per-line-parse workaround is sufficient - all 29 lines were read successfully despite line 27 failing strict `JSON.parse`.
  9. `constrained_scopes` is empty; nothing to reconsider this cycle.
- **L2 promotion gate, explicitly re-evaluated as requested**: NOT MET, and for the first time this is a real (not silent-zero) measurement. Summing `false_positives` across the 10 qualifying runs (11-20): non-zero on 3 of them (run 13, run 18, run 19 - each 1, all loop-derived, no human `[FP]` marks yet ever made) = **3 total, above the <=2 gate threshold**. Separately, **2 unresolved escalations are currently open** (`distinguish-broken-probe-from-dead-source`, and the newly-escalated `gate-cache-flag-on-min-volume`) against a gate requirement of 0. Recommendation: do not propose promotion. This is a materially different outcome than the 2026-08-06 retrospective, which found the gate "literally met" only because the false_positives counter had never been fed - it is now fed, and honestly does not clear the bar.
- Aether-OS local branch census (re-run live this retrospective per explicit request, `Desktop\Aether-OS` on work-it, `git branch -vv`): **16 total local branches - 1 (`master`) tracks `origin/master` live, 15 show `[gone]` remote tracking.** Corrects the Watch List's stale "14" figure (see Watch List entry) - reflects today's `closing-the-loop` deletion plus normal branch churn since the last full audit, not a simple 14-minus-1.

## Adjustment ledger
<!-- Seeded 2026-08-06 from the prose critiques of runs 1-10, which predate the structured notes.adjustment field. Retrospective step R1 reconciles this every 10th run by grepping LOOP.md and scripts/ - never by trusting this table's own text. Landed rows STAY here and get re-checked; a landed row that goes missing is REGRESSED and escalates. -->
| id | first_proposed | times | status |
|---|---|---|---|
| batch-branch-commit-date-lookups | 2026-07-14 | 5 | LANDED 2026-08-03 (branch_tips cache, step 1) |
| fix-spend-summary-date-window | 2026-07-17 | 1 | LANDED 2026-07-22 (scripts/spend-summary.mjs local-day bucketing) |
| record-output-token-baseline | 2026-07-17 | 1 | LANDED 2026-07-18 (step 3 notes) |
| flag-branches-20-commits-ahead | 2026-07-18 | 1 | LANDED 2026-08-06 (step 1, local hygiene - promotes a >20-ahead branch to High Priority) |
| branch-staleness-by-commits-ahead | 2026-07-21 | 1 | LANDED 2026-08-06 (step 1, GitHub - held earlier the same day, then applied by explicit decision; implemented as author-date staleness + `ahead_by` as a separate signal, NOT as the literal "replace date with commits-ahead", which would have removed the time dimension entirely) |
| verify-loop-own-commit-completed | 2026-07-24 | 1 | LANDED 2026-08-06 (retro refinement 6, step 5) |
| self-confirming-noise-without-fp-mark | 2026-07-24 | 1 | LANDED 2026-08-06 (retro refinement 1, step 2) |
| promote-tokenmonitor-pr1-to-human-decisions | 2026-08-03 | 2 | LANDED 2026-08-06 (Human Decisions section) |
| cache-quiet-repo-pr-issue-results | 2026-08-04 | 1 | LANDED 2026-08-06 (step 1, GitHub - held earlier the same day, then applied by explicit decision; implemented by ELIMINATING the per-repo sweep via one fleet-wide `gh search prs`/`gh search issues` call, NOT by caching a quiet-repo negative, which would have created a window where a new PR goes unreported) |
| drop-bare-uncommitted-changes-signal | 2026-08-04 | 1 | LANDED 2026-08-06 (retro refinement 5, step 1) |
| machine-tag-watchlist-items | 2026-08-06 | 1 | LANDED 2026-08-06 (retro refinement 4, step 2) |
| exclude-default-branches-from-staleness | 2026-08-06 | 1 | LANDED 2026-08-06 (step 1, GitHub - proposed by run 11's critique and applied same day; implemented literally) |
| gate-cache-flag-on-min-volume | 2026-08-07 | 3 | **DECLINED 2026-08-17 (human decision).** Escalated at attempt_cap (3) by the 2026-08-17 retrospective; 5 low-volume data points on record, 3-of-5 against the hypothesis (flagged: runs 12, 18; did not flag: runs 13, 17, 20). Retrospective recommended DECLINE; human confirmed same day. No LOOP.md change needed - nothing was ever applied for this adjustment. Not re-propose-eligible; a future run should not raise this again absent new evidence. |
| noise-match-on-finding-identity-not-text | 2026-08-10 | 1 | **LANDED 2026-08-17 (human decision, applied via loop-design skill).** Refinement 1's byte-identical-text noise match was silently inert for GitHub staleness findings, which embed an incrementing day-count that can never repeat byte-for-byte across runs. LOOP.md step 2 now matches on finding identity (repo + branch/path + recommendation, volatile fields like age stripped) instead of literal rendered text. |
| distinguish-broken-probe-from-dead-source | 2026-08-11 | 3 | **LANDED 2026-08-17 (human decision, applied via loop-design skill).** Escalated at attempt_cap after runs 14-16; user delegated the decision, agent assessed it as technically sound (consistently proposed, no contradicting evidence, stays within L1 read-only boundary, closes a real "broken probe reads as clean" reporting gap) and applied it. LOOP.md step 1 now distinguishes a dead/absent source (unchanged "source unavailable: <root>" line) from a multi-target source where 100% of targets fail the same way (new: abort, emit one `probe failure: <source> - <error>, N/N targets failed` line, never imply a clean result). |
| specify-branch-tips-cache-key-format | 2026-08-11 | 2 | **LANDED 2026-08-17 (human decision, applied via loop-design skill).** Canonicalized on the FULL 40-char SHA - the API call already returns it, so storing/comparing anything shorter was an unforced precision loss. LOOP.md's branch-staleness cache rules now say so explicitly and treat a shorter cached `sha` as a miss (same pattern as the earlier `date`/`author_date` fix). |
| dirty-count-blind-to-content-churn | 2026-08-13 | 1 | **LANDED 2026-08-17 (human decision, applied via loop-design skill).** Refinement 5's staleness check now compares the dirty file PATH SET, not just the line count, so a repo that sheds one file and gains a different one no longer reads as "unchanged." `notes.dirty_repos` schema changed `dirty_lines` -> `dirty_paths`; a cache entry still on the old field is treated as a miss. |
| expand-home-matt-discovery-root | 2026-08-15 | 1 | **HELD 2026-08-17 (human decision, delegated to the agent).** Not applied - this rests on a single home-matt-only observation (`~/projects/*` allegedly containing repos the current discovery roots miss) that cannot be independently verified from work-it, unlike the other 4 decided this run, which were self-contained mechanism fixes verifiable by reading the protocol/data directly. Re-propose with confirmation from an actual home-matt run (or a human directly checking that machine) rather than re-applying blind. |
| validate-jsonl-line-before-append | 2026-08-17 | 1 | **LANDED 2026-08-17 (human decision, applied via loop-design skill, same day as proposed).** Before appending a run's own line to `runs.jsonl` (step 3), round-trip it through `JSON.parse(JSON.stringify(...))` and abort/report rather than append if it fails. Directly motivated by run 19's line 27 (Windows-path backslash, un-escaped, sat undetected until this retrospective's full-file read). |

Ledger standing (reconciled 2026-08-17 retrospective; fully cleared same day across 6 human decisions): **17 of 19 landed**, 1 declined (`gate-cache-flag-on-min-volume`), 1 held (`expand-home-matt-discovery-root`), **0 outstanding**, **0 escalated at attempt-cap**. Every adjustment that was OUTSTANDING or ESCALATED as of the 2026-08-17 retrospective has a recorded human decision.

Pending since that reconciliation (in `runs.jsonl` `notes.adjustment` / `notes.outstanding_adjustments`, NOT yet ledger rows - R1 will collect them): `clarify-repo-discovery-depth-definition` (first_proposed 2026-08-18, 2x, OUTSTANDING - deliberately not re-proposed by run 23 to avoid tripping attempt_cap before anyone has decided it) and `freeze-unchanged-runs-when-not-verified` (first_proposed 2026-08-21, 1x, NEW from run 23).

Note for the next retrospective's step R1: two rows deviate from their original proposal text ON PURPOSE, and each row says how. When reconciling, check the file for what was ACTUALLY built, not for the phrase the critique used - `branch-staleness-by-commits-ahead` became author-date staleness plus a separate `ahead_by` signal, and `cache-quiet-repo-pr-issue-results` became a fleet-wide search that removes the calls rather than a cache of their answers. Both would read as never-landed under a naive text match. Also: when reading `runs.jsonl` in full for R1, line 27 is invalid JSON (see High Priority) - use a tolerant per-line parse that logs-and-skips a bad line rather than aborting the whole reconciliation.

## Watch List
- **Fleet PR/issue counts: 1 open PR, 1 open issue (both unchanged, both live-verified via GraphQL `gh search`).** [machine: any]
  - `TokenMonitorV2#1` "Reskin phases 3-4...", branches `reskin-phases-3-4` (19 ahead, unchanged) and `reskin-phase-5` (20 ahead, unchanged, still exactly at the escalation boundary and not over it), no new activity for **8 days** (`updatedAt` unchanged: 2026-08-13) - while `main` itself advanced today. [action: none, tracking - revisit if `reskin-phase-5` goes to 21+ or the PR passes ~14 days idle] [machine: any]
  - `Aether-OS#22` white screen after desktop lock. Still instrumented but NOT fixed, unchanged (`updatedAt` 2026-08-12). Deliberately left open. [action: none until it recurs - then grab the dev output and grep `[diag]` BEFORE closing the window] [machine: any]
- **`EFIPartitionRemediation` `feature/fleet-migration-runbook` - CROSSED the >14-day staleness line this run: author-date 2026-08-06, now 15 days old, 17 ahead of `master`.** Was tracked at 12d/13d for the two prior runs. Real unmerged content (17 ahead), not dead weight. The work-it clone of it (`Desktop\EFI-wt-migration`, checked out on this branch) is clean and fully pushed - so the exposure is "unmerged", not "unpushed". [action: review and either merge or explicitly abandon] [machine: any]
- **`TokenMonitor` `worktree-packages-core-wiring` - 17 days stale (author-date 2026-08-04, unchanged), 1 ahead of `master`.** Second consecutive run past the line. Verified via the GitHub API this run, not via a local clone. [action: review and either merge or explicitly abandon] [machine: any]
- **`Aether-OS` (work-it, `Desktop\Aether-OS`) carries 15 stale local branches whose remote tracking is `[gone]` (17 local branches total) - LIVE-VERIFIED this run via `git branch -vv`, unchanged, 5th consecutive run with zero human action.** Working tree clean, 0 unpushed. Already counted once as loop-derived noise (run 21); still undecided - see Untriaged noise. [action: optional bulk local cleanup (`git branch -d` each), not urgent] [machine: work-it]
- **NEW: `claude-token-tracker` (work-it, the `TokenMonitor` clone) carries 4 `[gone]`-tracking local branches of 9 total.** First census of this clone; working tree clean, 0 unpushed. Same benign shape as the Aether-OS item, smaller. [action: none yet, tracking - fold into any bulk gone-branch cleanup decision] [machine: work-it]
- `aether-os` (home-matt, `~/projects/aether-os`, a separate clone of the same `Aether-OS` repo) independently carries its own 3 stale/`[gone]`-tracking local branches (`comms-deck-stage14`, `master-ref`, `test-2026-07-24`). [action: optional cleanup, not urgent] [machine: home-matt, not verifiable on work-it]
- **CORRECTED: `Aether-OS-livetest` (work-it) - the "4 dirty lines unchanged for 7 consecutive runs" finding was FALSE and its flag is CLEARED.** Live check on work-it shows **5** dirty paths, not the single `package-lock.json` the cache held, and `package.json` is newly modified - the path set changed, so this is active WIP and refinement 5 suppresses it. Two home-matt runs incremented `unchanged_runs` 6 -> 7 for a repo neither could see. Separately: the clone is **101 behind `origin/master`**. [action: no staleness action; if anyone cares, the e2e specs and `livetest-shots/` are still untracked and the clone badly needs a pull] [machine: work-it]
- **NEW baseline: `NMMToolkit` (work-it) - 29 dirty paths, 0 unpushed, 0 behind.** Not reportable as a finding yet (refinement 5 needs 3 consecutive identical path sets); recorded so a later run can judge it. Note the volume: 26 of the 29 are `src/tools/**` scripts plus `build.ps1` and `tools.psd1`. [action: none this run, baseline only] [machine: work-it]
- `uw-mail-router` (work-it, `Downloads\uw-mail-router`) - 1 dirty (`sim/simulator.html`), 0 unpushed. Prior cache entry used the retired `dirty_lines` schema, so its counter was re-baselined to 0 per LOOP.md. [action: none] [machine: work-it]
- `TarotApp` (home-matt) - 3 unpushed, 8 ahead of `origin/master`, 3 dirty. [action: none, tracking] [machine: home-matt, not verifiable on work-it]
- `tarot` (home-matt) - 3 unpushed, no upstream tracking, 4 dirty. [action: none, tracking] [machine: home-matt, not verifiable on work-it]
- `Miriels-publish` (home-matt) - 1 unpushed, ahead 1, 3 dirty; same 3 paths as `tarot`'s dirty set (shared boilerplate). [action: worth checking whether this is one fix that should land in both repos] [machine: home-matt, not verifiable on work-it]
- `About-me` (home-matt) - 1 dirty (`README.md`). [action: none] [machine: home-matt, not verifiable on work-it]
- `nmmtools` (home-matt) - 1 dirty (`testResults.xml`). Known recurring test-artifact pattern. [action: none, known benign] [machine: home-matt, not verifiable on work-it]
- **`cli-shared-memory` (home-matt) - untracked `.claude/`, identical path set for 3 consecutive runs as of run 22.** Note: the work-it clone (`Desktop\cli-shared-memory`) is a DIFFERENT clone and is completely clean, so it is not evidence either way. [action: decide - commit, gitignore, or accept as standing clutter] [machine: home-matt, not verifiable on work-it]
- `TokenMonitorV2` (home-matt clone) - 0 unpushed, 1 dirty (`.claude/`). The work-it clone (`Desktop\TokenMonitorV2`) is clean and level with `origin/main`. [action: none] [machine: home-matt, not verifiable on work-it]
- `agent-improvement` store (home-matt) - untracked `prototyping-tasks/` directory, out of scope for this loop's own writes. Absent on work-it. [action: none from this loop] [machine: home-matt, not verifiable on work-it]
- `agent-improvement` candidate buffer - **work-it buffer is 1 pending line** (live). The home-matt buffer's 277 pending lines are in a machine-local gitignored file and are not visible from here. [action: home-matt should run a promote pass] [machine: per-machine; home-matt count not verifiable on work-it]
- `.agency-agents` and `claude-power-automate` (home-matt) - clean, non-`mwgrant21` remotes, outside fleet PR/issue scope. `claude-power-automate` also exists on work-it (`~/claude-power-automate`) and is clean/level there. [action: none] [machine: home-matt for the first, both for the second]
- Literal tilde-named directory at `C:\Users\Matt\~\vexjoy-agent` holding a clean clone of `notque/vexjoy-agent`. No equivalent exists on work-it. [action: none required, worth a rename if unintentional] [machine: home-matt, not verifiable on work-it]
- Desktop stray `.git` (`C:\Users\IT\Desktop`, work-it only): known/by-design; skipped, not re-raised. [action: none] [machine: work-it]

## Recent Noise (ignored this run)
<!-- Mark an item [FP] if it was a false positive. Refinement 1: the loop ALSO counts an item whose FINDING IDENTITY (volatile fields like age stripped) is unchanged across 3 consecutive runs with no human action as noise on its own evidence, without waiting for a mark. -->
- Spend threshold: today's **2,722** output tokens - far under the 750,000 absolute floor, so the two-part AND gate cannot fire. No flag.
- Cache threshold: today's **80.7% - FLAGGED.** Below both the 0.90 absolute floor and the median-minus-5pp floor (0.911, from a 0.961 five-run median). Context: a 2,722-output-token day on a fresh session (337k cacheRead / 81k cacheCreate), i.e. the low-volume shape that adjustment `gate-cache-flag-on-min-volume` would have suppressed. That adjustment was **DECLINED by human decision 2026-08-17** and is deliberately NOT re-proposed - the flag is reported as the protocol currently specifies.

## Untriaged noise
(none - both items were decided by the human on 2026-08-21; see Human Decisions and Resolved)

## Human Decisions (overrides the loop must respect)
- TokenMonitor PR #1 ("Terminal project folder + repo CLAUDE.md") was the user's own active PR, tracked presence/staleness only for 9 runs. **The PR is now MERGED**, so the override is satisfied and retired. Recorded rather than silently deleted so a future run does not read its absence as the decision having been lost.
- **2026-08-17: adjustment `gate-cache-flag-on-min-volume` DECLINED.** The 2026-08-17 retrospective escalated it at attempt_cap (3) and recommended decline given evidence turned 3-of-5 against the hypothesis. Human confirmed decline the same day. See Adjustment ledger for the full evidence trail. A future run/retrospective should treat this as closed, not re-propose it absent materially new evidence (e.g. a longer run of low-volume days).
- **2026-08-17: adjustment `distinguish-broken-probe-from-dead-source` APPLIED.** Escalated at attempt_cap after runs 14-16 with no retrospective recommendation either way; user delegated the actual accept/decline call to the agent, which applied it after assessing it as technically sound and within the L1 boundary. See LOOP.md step 1 and the Adjustment ledger for the change itself and reasoning.
- **2026-08-17: remaining 5 OUTSTANDING adjustments all decided in one pass, user delegation ("decide the remaining outstanding adjustments too").** 4 APPLIED, 1 HELD:
  - `noise-match-on-finding-identity-not-text` - APPLIED. Self-contained logic bug (byte-identical text match can't fire on findings with an incrementing day-count), verifiable directly from LOOP.md's own text.
  - `specify-branch-tips-cache-key-format` - APPLIED. Canonicalized on the full 40-char SHA the API already returns; same failure class as the already-fixed `date`/`author_date` ambiguity.
  - `dirty-count-blind-to-content-churn` - APPLIED. Line-count comparison replaced with file-path-set comparison for the stale-uncommitted-changes check.
  - `validate-jsonl-line-before-append` - APPLIED (landed same day as proposed). Round-trip validation before appending to `runs.jsonl`, directly motivated by the confirmed line-27 corruption.
  - `expand-home-matt-discovery-root` - **HELD, not applied.** The one adjustment of the five resting on an empirical claim about a different machine's filesystem (`~/projects/*` on home-matt) that cannot be verified from work-it. The other four were mechanism-level fixes verifiable by reading the protocol/data directly in front of the agent; this one is not, so it was held rather than applied blind. Re-propose with home-matt confirmation.
  - See Adjustment ledger for full per-item reasoning; ledger now 17 landed / 1 declined / 1 held / 0 outstanding / 0 escalated.
- **2026-08-21: `Aether-OS` (work-it) 15 `[gone]`-tracking local branches - bulk cleanup APPROVED and ALREADY EXECUTED.** Done out-of-band by the main session on work-it, not by this loop (L1 boundary intact - the loop reported, a human authorized, a separate session acted). Verification before deletion: `git fetch --prune`; `master` pulled `aa7d218` -> `9803c5c` and is level with origin; `git log --branches --not --remotes` empty (zero unpushed commits anywhere); `git branch --no-merged master` empty (all 16 branches fully merged). All 15 deleted with `git branch -d` (the form that refuses unmerged work), never `-D`. The clone now holds **1 branch, `master`**: later the same day the human also authorized deleting `chore/electron-43.4.1`, the one merged branch whose remote still exists. It too was verified merged, with `origin/master..origin/chore/electron-43.4.1` empty and local/remote at the same SHA `f460817`, then deleted with `git branch -d`. The **remote** branch `origin/chore/electron-43.4.1` was then deleted too, on separate explicit authorization (outward-facing, so it was confirmed rather than assumed); `gh pr list` confirmed no open PR referenced it first. Aether-OS is now `master`-only locally. The two other remote branches were then deleted as well on further explicit authorization, after confirming both were `ahead_by 0` against master (fully absorbed, no unmerged work) and that no open PR referenced either: `project-scope-switch` (`e8d1c20b148a566745a2bd93a27fa4a21158096f`, author-date 2026-08-11) and `real-projects-view-stage16` (`208b26e0bcac3b078ad5e6c98b6f7ef6be509746`, author-date 2026-08-08). SHAs recorded here because a deleted remote branch is recoverable by SHA; both are ancestors of `master` regardless, so no content was at risk.

  **Aether-OS end state 2026-08-21: exactly one branch, `master`, local and remote.** It went from 17 local + 4 remote branches to 1 + 1 in a single day. A future run should treat ANY new branch there as new work, not as leftover clutter - the baseline is now zero. Note for the branch-tips cache: this run's `notes.branch_tips` map for Aether-OS was captured BEFORE these deletions, so run 24 will see 3 remote branches disappear at once; that is this authorized cleanup, not a probe failure. Do not re-raise; if `[gone]` branches accumulate again that is a NEW finding, not this one.
- **2026-08-21: `cli-shared-memory` (home-matt) untracked `.claude/` - DECISION IS `commit it`. NOT YET DONE.** The human chose commit over gitignore/accept. It could not be executed on 2026-08-21 because the finding is home-matt-only: the work-it clone of `cli-shared-memory` has no `.claude/` directory at all and `git status --porcelain --untracked-files=all` returns completely empty, verified that day. Loop instruction: STOP asking for a decision on this item - the decision exists. Carry it instead as a **pending human action, machine-tagged `[machine: home-matt]`**, and mark it Resolved only once a run FROM home-matt observes the `.claude/` path set actually gone from that clone's untracked set. A run from work-it must report it `not verifiable on work-it` per refinement 4 and must not infer completion from its own clone being clean.

## Resolved since last run
<!-- Pruned each run per step 2. Prior entries remain in git history. -->
- **`EFIPartitionRemediation` `feature/fleet-migration-runbook` - RESOLVED 2026-08-21 by MERGE** (run 23 Watch List item, "15d stale, 17 ahead, merge or explicitly abandon"). Human chose merge. Fast-forward `e5f1ca7..c77a650` (master was 0 behind, and this repo's master has no merge commits - linear history is the house style), pushed, master level with origin. Verification before merging used the repo's documented runner, **Pester 3.4.0 under `powershell.exe`** - NOT the Pester 5.7.1 also installed on this box, under which these Pester-3-era tests collapse with "Legacy Should syntax is not supported". Branch **120/122**; master control **59/61**. The branch adds 61 tests, all passing, and introduces **zero new failures**: the same 2 tests fail on both sides and both are elevation gates (`Invoke-EFIAutopilot.ps1` carries `#requires -RunAsAdministrator`; the other writes to `HKLM`), not defects. The branch was NOT deleted - it is checked out in the live worktree `~/Desktop/EFI-wt-migration`, so deleting it would break that worktree. Separate decision, not part of this resolution.
- **Test-residue defect found while verifying the above (REPORTED, NOT FIXED).** `tests/EFIMigrationState.Tests.ps1`'s ACL failure test applies a `Deny | SetValue` ACE to `HKCU:\Software\EFIMigrationTest\AclTest` and removes it only in a `finally`. A previous run on work-it left that ACE in place, which **poisoned every later run**: the test could no longer recreate its own scratch key, and the failure surfaced as "Access to the registry key is denied" - reading as a broken environment rather than as leftover test state. The task-8 report for that commit claims "Cleanup verified: No leftover `HKCU:\Software\EFIMigrationTest` keys or ACL rules remain after test suite completes"; that claim was false on this machine. Cleared by hand to complete the verification. [action for a human: strip any pre-existing deny ACE in the test's SETUP, so a killed run cannot poison the next one]
- **Fleet-wide branch cleanup 2026-08-21 (human-authorized, executed out-of-band on work-it).** Applied the Aether-OS rule to every other repo: delete only branches that are fully merged (local, via `git branch -d`, never `-D`) or `ahead_by 0` (remote, and only with no open PR. Survey covered all 23 GitHub repos and all 15 local clones. Result - the qualifying set was far smaller than the branch counts suggest:
  - **8 local branches deleted.** `TokenMonitorV2`: `worktree-reskin-phases-3-4` (gone + merged). `claude-token-tracker` (origin = `mwgrant21/TokenMonitor`): `design-v2-phase1`, `fix/live-feed-follows-active-session`, `reframe-model-routing-finding`, `token-tracker-impl` (all gone + merged), plus `design-v2-phase3` and `worktree-packages-core-task2` (no upstream at all, both merged) and `chore/electron-43.4.1`. This RESOLVES run 23's "NEW: `claude-token-tracker` (work-it) 4 `[gone]` branches of 9" finding - that clone now holds `master` plus one worktree branch. `TokenMonitorV2`'s `main` was pulled from 31 behind to level first.
  - **Only 1 remote branch fleet-wide qualified**: `TokenMonitor/chore/electron-43.4.1` (ahead=0, behind=1, no open PRs). Its deletion was initially blocked by the harness permission classifier; **the human ran the `git push origin --delete` themselves and it succeeded** - branch gone, confirmed by a post-delete `gh api .../branches` sweep. CLOSED.
  - **Nothing else was eligible, and nothing carrying work was touched.** Every other non-default remote branch fleet-wide is ahead of its default: `TokenMonitorV2` `reskin-phase-5` (20), `reskin-phases-3-4` (19, and it is the head of open PR #1), `chore/postinstall-install-electron` (1); `TokenMonitor` `req-4-stryker-ci-gate` (1), `worktree-packages-core-wiring` (1); `EFIPartitionRemediation` `feature/fleet-migration-runbook` (17). The last two are checked out in live worktrees. The two "merge or abandon" Watch List items from run 23 are therefore UNCHANGED and still open - cleanup did not touch them.
  - Note for run 24's `branch_tips` diff: `claude-token-tracker`/`TokenMonitor` and `TokenMonitorV2` will show local branches gone; this is the authorized cleanup, not a probe failure.
- **`Aether-OS` (work-it) 15 stale `[gone]`-tracking local branches - RESOLVED 2026-08-21 by human-authorized cleanup** (after run 23's digest, before run 24). All 15 deleted with `git branch -d` after confirming every one was fully merged into an up-to-date `master` and that the repo had zero unpushed commits. 2 branches remain. Full evidence and the standing override in Human Decisions.
- **`Aether-OS-livetest` stale-WIP flag RESOLVED as a FALSE FINDING**, not as fixed work. See the corrected Watch List entry: the path set is 5 paths, not the 1 the cache held, and it changed since last observation. Counter re-baselined to 0, flag cleared. This is the evidence behind this run's adjustment `freeze-unchanged-runs-when-not-verified`.
- **`TokenMonitorV2` `worktree-reskin-phases-3-4` branch - RESOLVED.** Deleted from the remote (present in run 22's `branch_tips`, absent from this run's listing).
- **`TokenMonitorV2` `chore/postinstall-install-electron` - RESOLVED down to 1 ahead** (was 6 ahead); `main` advanced today and absorbed the work.
- `agent-improvement` domain freshness: newest `Added:` date across `domains/*.md` is **2026-08-21 (today)**, 0 days old - well inside the >7-day note threshold. A promote pass ran on this machine earlier today.
- `agent-improvement` store: confirmed clean and level with `origin/master` at the start of this run, with no out-of-scope untracked files on work-it.
- SPEND: no flag - see Recent Noise.
- CACHE: **FLAGGED** this run - see Recent Noise. (Recorded here so the flag is not lost between sections; it is the only threshold that fired.)
